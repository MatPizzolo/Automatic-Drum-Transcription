# Test dependencies — install on top of requirements-api.txt
# For ML-dependent tests (onset_detection, transcription), also install requirements-worker.txt
-r requirements-api.txt

pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-mock>=3.12
httpx>=0.27
anyio[trio]>=4.0
